---
id: tuning
title: Setup · Tuning 옵션 전체 안내
description: RealSweatSystem 1.1.0의 일반 Setup 및 Tuning 옵션 레퍼런스입니다.
---

# Setup · Tuning 옵션 전체 안내

이 페이지는 RealSweatSystem 1.1.0에서 사용자가 정상적으로 조절하는 옵션을 정리합니다. **Base** 표시는 Sweat Base Module, **Effects** 표시는 Sweat Effects Module이 설치된 경우에 나타나는 항목입니다.

## Tuning 컴포넌트 열기

Setup을 완료한 뒤 Hierarchy에서 아바타를 펼치고, 아바타 루트 바로 아래에 생성된 **RealSweat** 오브젝트를 선택합니다. Inspector의 **RealSweatSystem Tuning**에서 값을 조절할 수 있습니다.

`RealSweat` 오브젝트가 최초 Setup 실패로 생성되지 않았다면 Console의 첫 오류를 해결한 뒤 **Setup — 자동 설치 / 업데이트**를 다시 실행하세요. 완료된 설치의 오브젝트를 직접 삭제했다면 Setup을 실행하기 전에 [수동 삭제 복구](../troubleshooting.mdx)를 먼저 확인하세요.

## 변경값이 반영되는 시점

| 변경 항목 | 반영 시점 |
|---|---|
| **전체 땀양** | 머티리얼 품질은 즉시 바뀝니다. 젖음 애니메이션·낙하 물방울·몸 김까지 맞추려면 **FX에 적용**을 누릅니다. |
| **물방울 크기 / 패턴 밀도** | Tess와 No Tess 표면 머티리얼에 즉시 반영됩니다. 낙하 물방울과 강한 움직임 비산 크기까지 갱신하려면 **FX에 적용**을 누릅니다. |
| **입체 표현 정밀도** | 설치된 Tess 머티리얼에 즉시 반영됩니다. |
| 자동 반응·누적 곡선·유지시간·이펙트·고급 FX 값 | **FX에 적용**을 누릅니다. |
| 바디 머티리얼의 Shadow Strength | **FX에 적용** 또는 Setup 재실행 시 복사됩니다. |
| Custom Steam의 발생량·퍼짐·방향 등 | **FX에 적용** 또는 Setup 재실행 시 검증·생성 에셋이 갱신됩니다. |
| 대상 Body·Element·추가 대상·2D/3D·렌더 순서·테셀레이션 지원 등 설치 구조 | **Setup — 자동 설치 / 업데이트**를 다시 실행합니다. |

누적 그래프는 편집 중인 값을 즉시 미리 보여주지만, **FX에 적용**을 누르기 전에는 생성된 FX가 바뀌지 않습니다.

## Setup 창 옵션

| 항목 | 모듈 | 기본값 | 의미 |
|---|---|---:|---|
| **아바타** | 공통 | 미지정 | VRC Avatar Descriptor가 있는 대상 아바타입니다. |
| **바디 메쉬** | 공통 | 자동 감지 | Hierarchy에서 피부용 `SkinnedMeshRenderer`가 붙은 메인 Body 오브젝트입니다. Project 창의 `.fbx`나 Mesh 에셋이 아닙니다. |
| **땀 적용 Element** | Base | 자동 후보 | 다시 렌더링할 Body 머티리얼 / SubMesh 영역입니다. 표시되는 재질명을 확인해 피부 슬롯을 선택하며, 같은 Renderer의 여러 Element를 선택할 수 있습니다. |
| **실험적 추가 대상** | Base | 없음 | 얼굴 등 별도 SkinnedMeshRenderer와 대상 Element를 추가합니다. 대상마다 크기·밀도·마스크·Tess와 선택형 낙하 물방울을 따로 설정합니다. |
| **렌더 순서 자동 설정 (권장)** | Base | 켜짐 | 땀 Render Queue를 정확히 `2455`로 설정합니다. 선택한 Body 머티리얼은 `2454` 이하여야 합니다. |
| **수동 땀 렌더 큐** | Base | `2455` | 자동 렌더 순서를 끄면 나타납니다. Body 머티리얼보다 높은 값으로 설정합니다. |
| **바디 Shadow Strength 따라가기** | Base | 켜짐 | Setup 또는 **FX에 적용** 실행 시 설정된 각 Body Element와 실험적 추가 대상의 원본 `_ShadowStrength`를 해당 땀 재질에 복사합니다. lilToon과 Poiyomi를 지원합니다. |
| **젖은 피부 광택 사용** | Base | 켜짐 | Sweat %에 따라 증가하는 젖은 막·광택 표현을 생성 마테리얼에 적용합니다. |
| **거울에서도 바디 땀 전체 품질** | Base | 꺼짐 | 거울의 물방울 밀도·광학·Light Volumes·Tess/No Tess 품질을 메인 화면과 같게 복원합니다. 거울 GPU 중～고부하 옵션입니다. |
| **전체 땀양** | 공통 | 적음(저부하) | 표면 계산 예산과 젖음 애니메이션·이펙트 양의 기준을 선택합니다. |
| **자동모드 감도** | 공통 | 균형 | 작은 신체 움직임에 Auto Mode가 얼마나 민감하게 반응할지 선택합니다. |
| **Write Defaults 호환 방식** | 공통 | 자동 감지 (권장) | 외부 FX가 모두 WD ON일 때만 RealSweat도 모두 WD ON으로 구성하고, 그 외에는 Driver ON / Output OFF 호환 구조를 사용합니다. |
| **입체기능(테셀레이션)(고부하) 설치하기** | Base | 꺼짐 | 끄면 No Tess 하나만, 켜면 Tess와 No Tess를 함께 넣고 인게임 입체기능 전환을 추가합니다. |
| **입체 표현 정밀도** | Base + Tess | 간소화 | 설치되는 Tess 머티리얼의 정밀도입니다. |
| **입체기능(테셀레이션) 기본값 켜기** | Base + Tess | 꺼짐 | 아바타 로드 직후 로컬 입체 표시의 초기 상태입니다. 각 관리 슬롯마다 Tess／No Tess 중 하나만 활성화됩니다. |
| **3D 파티클 품질** | Effects | 꺼짐 | 입김·낙하 물방울·몸 김을 메시 파티클로 설치합니다. 끄면 기존의 가벼운 2D를 사용합니다. |
| **물방울 충돌 튀김** | Effects + 3D | 켜짐 | 3D 낙하 물방울이 표면에 충돌할 때 작은 방울이 위로 튀게 합니다. |
| **낙하 물방울** | Effects | 켜짐 | 몸에서 떨어지는 땀 파티클을 설치합니다. |
| **바닥 물자국 사용** | Effects | 켜짐 | 낙하 물방울이 바닥에 남기는 젖은 자국을 설치합니다. |
| **입김** | Effects | 켜짐 | 입김 emitter를 설치합니다. |
| **내 1인칭에서 입김 보이기** | Effects + Breath | 꺼짐 | MA Visible Head Accessory로 본인 1인칭에도 입김을 표시합니다. |
| **몸 김** | Effects | 켜짐 | 몸 김 emitter를 설치합니다. |
| **몸 김 위치** | Effects | 가슴골·양어깨·겨드랑이·허벅지 켜짐, 고간·발 꺼짐 | Setup이 생성할 몸 김 위치를 선택합니다. |
| **몸 김 거울 모드** | Effects + Steam | Low | `Full / Low / Hidden` 중 거울 표시를 선택합니다. 3D Low는 현재 Full과 같은 부하입니다. |
| **몸 김·입김 진하기 메뉴 설치** | Effects | 꺼짐 | 몸 김／Custom Steam과 입김의 불투명도를 함께 조절하는 저장·동기화 Float 래디얼을 추가합니다. |

2D/3D 파티클 품질은 Setup이 파티클 구조를 다시 생성하므로 **FX에 적용**만으로 전환할 수 없습니다. 바닥 물자국은 낙하 물방울의 충돌 하위 이펙트이므로 낙하 물방울이 설치되고 켜져 있어야 합니다.

**테셀레이션**은 Base의 피부 표면 물방울 변위를 만드는 기능이고, **3D 파티클 품질**은 Effects의 입김·낙하 물방울·몸 김 렌더링 방식입니다. 서로 독립된 옵션이며 신규 설치에서는 둘 다 기본 OFF입니다.

### 실험적 여러 Element와 추가 대상

- 메인 Body Renderer의 여러 Material Element를 선택하거나 별도 `SkinnedMeshRenderer`를 추가할 수 있습니다.
- Rest Position／Normal／Tangent는 Material Element마다 별도로 만드는 데이터가 아니라 **Mesh의 버텍스 구성별 데이터**입니다. 같은 Mesh의 여러 Element는 같은 Rest 기준을 사용하고, 서로 다른 Mesh는 Rest 데이터를 공유할 수 없습니다.
- 대상마다 별도 땀 마테리얼과 드로우콜이 추가됩니다. 추가 대상은 Read/Write 가능한 Mesh, 유효한 Submesh·Material·Tangent, 고유한 GameObject/애니메이션 경로와 다른 대상이 공유하지 않는 고유한 땀 마테리얼이 필요합니다.
- 추가 대상은 크기·밀도·스폰/표시 마스크·Tess/No Tess를 따로 설정합니다. Effects가 있으면 대상별 낙하 물방울과 표면적·발생률도 선택할 수 있습니다.
- Face Tracking·표정·Mesh 병합·교체·최적화의 모든 조합은 보장되지 않습니다. 눈·입술 주변 Tess는 권장하지 않습니다.
- RealSweat이 만든 마테리얼이나 Renderer 슬롯을 직접 삭제·비우기·이동·교체·병합하지 마세요. 이는 대상 제외가 아니라 설치 손상으로 처리되고 빌드가 중단됩니다.
- 제거하거나 복구하려면 Tuning의 **추가 대상 관리...** 또는 **Setup에서 복구...**로 현재 목록을 연 뒤 전체 Setup을 실행합니다.

### Custom Steam Point

Setup의 Custom Steam 목록에 아바타 하위 Transform을 추가하면 크기·발생량·퍼짐·방향과 좌우 미러 페어를 설정할 수 있습니다. 활성 커스텀 슬롯은 최대 4개이며 미러 페어는 2개를 사용합니다. 내장 위치와 합쳐 10개를 넘으면 거울 부하 경고가 표시되고 총 12개가 상한입니다. 발생량 가중치 합계가 `4.0`을 넘으면 모든 이미터가 같은 비율로 줄어듭니다.

### Write Defaults 호환 방식

- **자동 감지 (권장):** 외부 FX가 모두 WD ON일 때만 RealSweat의 모든 State도 WD ON으로 구성합니다. WD OFF, 혼합 또는 판정 불가 구성은 WD OFF 호환 구조를 사용합니다.
- **WD ON 호환:** RealSweat의 모든 State를 WD ON으로 고정합니다. 전체 WD ON FX가 필요하다는 것을 알고 있는 아바타 또는 MMD 환경에서만 사용하세요.
- **WD OFF 호환:** WD OFF 또는 혼합 아바타를 위해 v1.0.1의 Driver ON / Output OFF 분리 구조를 유지합니다.

### 전체 땀양

- **적음(경량화):** 물방울 계산 예산이 가장 작습니다. 일부 신체 부위나 시야각에서 패턴이 이어지는 곳에 옅은 선이 보일 수 있습니다.
- **적음(저부하):** 기본값입니다. 낮은 부하를 유지하면서 경량화 단계의 이음선을 숨기는 쪽입니다.
- **보통(중부하):** 땀 표시량과 표면 계산 예산이 증가합니다.

업로드 전에 근접 화면과 거울을 모두 확인하세요. 땀양·패턴 밀도·화면 점유율·테셀레이션은 GPU 부하가 서로 겹칠 수 있습니다.

### 테셀레이션 선택지

| 입체 표현 정밀도 | Tess 계수 | 용도 | 상대 부하 |
|---|---:|---|---|
| **간소화 — 일반 플레이용** | `2.5` | 일반 플레이 | Tess 중 가장 낮음 |
| **균형 — 근접 감상용** | `4` | 가까운 거리 | 높음 |
| **정밀 — 사진 촬영용** | `7` | 정지 사진 | 가장 높음 |

테셀레이션 지원을 설치해도 각 관리 슬롯에서 Tess와 No Tess가 동시에 렌더링되지는 않습니다. 인게임 메뉴는 Tess 지원으로 관리되는 모든 땀 슬롯을 한꺼번에 Tess 또는 No Tess로 전환합니다. 입체기능을 끄면 실제 No Tess 셰이더를 사용하므로 Hull／Domain 테셀레이션 연산도 실행되지 않습니다.

## Tuning 옵션

### 자동 누적 반응

| 항목 | 기본값 | 의미 |
|---|---:|---|
| **감도 프리셋** | 균형 | **균형**은 움직임 강도에 비례하고, **민감**은 작은 움직임에서 더 빨리 최대 반응에 도달합니다. 프리셋을 바꾸면 반응 시작점·포화점·상승 / 건조 기준·평활화가 해당 프리셋 값으로 돌아갑니다. |
| **땀 축적 배속** | `1.0` | 프리셋 전체의 자동 누적 속도 배율입니다. 범위 `0.25–4.0`. |
| **수평 민감도 (좌우·앞뒤)** | `1.0` | 좌우 및 앞뒤 움직임 반응입니다. 범위 `0.95–1.10`. |
| **상하 민감도** | `1.2` | 위아래 움직임 반응입니다. 범위 `0.90–1.30`. 작은 상하 운동이나 스쿼트에는 `1.2`를 권장합니다. |
| **미리보기 방향** | 혼합 | 그래프 미리보기만 바꿉니다. 인게임에서는 모든 방향이 계속 활성화됩니다. |
| **움직임 반응** | 접힘 | 현재 시작점·포화점·평활화·방향 민감도를 보여주는 미리보기입니다. |
| **누적 프로파일** | 균일 | 현재 Sweat % 구간에 따라 누적 속도를 바꿉니다. 동기화 파라미터나 런타임 레이어를 추가하지 않습니다. |
| **세부 수치** | 접힘 | 활성 곡선의 `0/25/50/75/100%` 배율을 편집합니다. 범위 `0.1–2.0`. |
| **누적 그래프** | 접힘 | 일정한 움직임을 가정한 곡선과 95% 도달 예상시간을 보여줍니다. 실제 PhysBone 입력은 아바타와 애니메이션에 따라 달라집니다. |

누적 프로파일:

- **균일:** 모든 Sweat %에서 같은 배율을 사용합니다. 신규 설치의 기본값입니다.
- **자연스럽게 (권장):** 초반은 조금 빠르고 Sweat %가 높아질수록 점차 느려집니다.
- **초반 빠르게:** 시작 구간의 누적을 강하게 강조합니다.
- **후반 가속:** 초반은 느리고 후반으로 갈수록 빨라집니다.
- **사용자 지정:** 다섯 지점을 직접 편집합니다.

제공되는 프로파일은 95%까지의 예상시간이 거의 같게 조정되어 있지만, 보이는 진행 방식과 마지막 5%는 다를 수 있습니다. 감도·축적 배속·방향 민감도·누적 프로파일은 모두 Auto Mode에 함께 영향을 줍니다.

### 표면 및 공통 옵션

| 항목 | 모듈 | 기본값 | 의미 / 부하 |
|---|---|---:|---|
| **전체 땀양** | 공통 | 적음(저부하) | Setup과 같은 3단계입니다. 머티리얼은 즉시, 생성 FX는 **FX에 적용** 후 갱신됩니다. GPU 영향은 중간~높음입니다. |
| **물방울 크기** | Base | `1.35` | 몸 표면 물방울 크기입니다. 범위 `0.5–2.0`. 직접 비용은 낮지만 덮는 면적이 커지면 효과가 더 눈에 띕니다. |
| **패턴 밀도** | Base | `24` | 높을수록 셀이 작고 촘촘해집니다. 범위 `4–64`. 사람이 많거나 근접한 환경에서는 적당한 값을 권장합니다. |
| **입체기능 기본값 켜기** | Base + Tess | 꺼짐 | 로드 직후 로컬 입체 표시입니다. 추가 드로우를 만들지는 않습니다. |
| **입체 표현 정밀도** | Base + Tess | 간소화 | Tess 정밀도입니다. 정밀도가 높아질수록 GPU 부하가 크게 증가합니다. |
| **바디 Shadow Strength 따라가기** | Base | 켜짐 | 실시간 연결이 아니라 값을 복사합니다. Body를 바꾼 뒤 **FX에 적용**을 누르세요. |
| **젖은 피부 광택 사용** | Base | 켜짐 | 젖은 막과 하이라이트를 함께 켜고 끕니다. 업데이트 시 기존 설치 마테리얼 상태가 유지됩니다. |
| **거울에서도 바디 땀 전체 품질** | Base | 꺼짐 | 거울에 메인 화면과 같은 표면 품질을 복원합니다. No Tess는 중부하, Tess는 고부하입니다. |
| **땀 유지시간 배율** | 공통 | `1.0` | 건조 시간 배율입니다. 높일수록 땀이 오래 남습니다. 범위 `0.5–2.0`. |

### 이펙트 켜기 / 끄기

이 항목은 **Effects Module**이 설치된 경우에만 나타납니다.

| 항목 | 기본값 | 의미 |
|---|---:|---|
| **낙하 물방울** | 켜짐 | 몸에서 떨어지는 땀을 활성화합니다. |
| **블룸 반짝임** | 켜짐 | 낙하 물방울에 절제된 HDR 하이라이트를 더합니다. Bloom을 사용하는 월드에서만 번집니다. |
| **바닥 물자국 사용** | 켜짐 | 낙하 물방울이 만드는 젖은 자국을 활성화합니다. 자국 생성에는 낙하 물방울도 필요합니다. |
| **입김** | 켜짐 | 입김 emitter를 활성화합니다. |
| **몸 김** | 켜짐 | 몸 김을 활성화합니다. |
| **몸 김 위치** | 가슴골·양어깨·겨드랑이·허벅지 켜짐 | 설치된 emitter를 활성화합니다. 고간과 발은 기본 꺼짐입니다. Setup에서 생성하지 않은 위치를 추가하려면 Setup을 다시 실행하세요. |
| **몸 김 거울 모드** | 새 설치 `Low` | 2D에서 `Full / Low / Hidden`을 선택합니다. 3D Low는 현재 Full과 같은 표현 및 부하입니다. |
| **몸 김·입김 진하기** | 설치 시 `50%` | 선택 설치한 경우 몸 김／Custom Steam과 입김 불투명도를 0.1～1.5배로 함께 조절합니다. 50%는 기존 표현을 유지하는 1.0배이며, 파티클 수와 성능 부하는 줄지 않습니다. |

## 고급 FX 조정

고급 옵션은 생성된 Animation / Particle 에셋을 수정하므로 **FX에 적용**이 필요합니다. **분류 기본값 복원**은 해당 분류만 초기화합니다.

### 자동 반응

| 항목 | 기본값 | 의미 |
|---|---:|---|
| **반응 시작점** | 균형 `0.04`, 민감 `0.015` | 자동 누적이 시작되는 움직임 크기입니다. |
| **포화점** | 균형 `0.35`, 민감 `0.18` | 상승 속도가 최대 반응에 도달하는 움직임 크기입니다. |
| **평활화** | 균형 `0.12`, 민감 `0.16` | 높을수록 빠르게, 낮을수록 묵직하게 반응합니다. |
| **땀 누적 저항 (%)** | `100%` | 높은 Sweat %의 누적을 점진적으로 억제합니다. 강한 움직임은 최대 효율을 유지하며 `0%`는 이전 방식의 반응입니다. |
| **이동 재개 대기시간** | `1초` | WASD / Jump가 멈춘 뒤 진자 움직임으로 다시 누적되기까지의 시간입니다. |
| **물방울 회복시간** | `3초` | 움직임이 멈춘 뒤 낙하 물방울 활동이 안정되는 시간입니다. |
| **운동 회복시간** | `9초` | 몸 열기에 사용되는 중간 운동 단계의 회복시간입니다. 입김 표시 자체를 뜻하지 않습니다. |
| **몸 열기 회복시간** | `30초` | 몸 김 주기 보정의 회복시간입니다. |
| **진자 본** | Hips | 모션 센서 부착 위치입니다. Chest·Head·좌우 Hand도 선택할 수 있지만 Head / Hand는 민감 프리셋에서 과민할 수 있습니다. |

### Effects Module 고급 분류

| 분류 | 조절 항목 | 분류 기본값 |
|---|---|---|
| **낙하 물방울** | 양·낙하속도·수명·크기·길이 배율, 강한 움직임 비산, 비산 임계값·확률·쿨다운·최소 / 최대 개수 | 배율 `1.0`, 비산 켜짐, 임계값 / 확률 `0.55`, 쿨다운 `3.5초`, `2–5개` |
| **바닥 물자국** | 수명·크기·불투명도, 중앙 건조 대비, 고임 확률 | 배율 `1.0`, 대비 `0.75`, 신규 기본 확률 `0` |
| **몸 김** | 시작 Sweat %, 밀도·상승속도·수명·크기·퍼짐·불투명도 | 시작 `30%`, 배율 `1.0` |
| **입김** | 시작 Sweat %, 입 열림 방식·파라미터·임계값, 양·속도·수명·크기·퍼짐·불투명도·주기 속도 | 시작 `25%`, Auto, `v2/JawOpen`, 입 열림 `0.2`, 배율 `1.0` |
| **성능 한도** | 낙하 물방울·바닥 자국·몸 김·입김의 최대 파티클 수 | 낙하 `64`, 자국 `80`, 몸 김 emitter당 `24`, 입김 `48` |

**입김 입 열림 방식**:

- **항상:** 입 움직임으로 제한하지 않습니다.
- **Viseme (립싱크):** VRChat Viseme을 사용합니다.
- **사용자 파라미터 (VRCFT):** 지정한 Float와 입 열림 임계값을 사용합니다.
- **자동 (VRCFT 우선, Viseme 대체):** 변화 중인 VRCFT `JawOpen`을 우선 사용하고, 8초 동안 열림 / 닫힘 변화가 없으면 Viseme으로 대체합니다.

## 땀 머티리얼 맵

아래 항목은 Tuning 컴포넌트가 아니라 생성된 RealSweat 머티리얼 Inspector에서 조절합니다.

| 항목 | 위치 | 용도 | 중요 설정 |
|---|---|---|---|
| **스폰 마스크 (흰색 = 생성)** | **기본 > 스폰 마스크** | 흰색은 물방울 생성, 검은색은 차단, 회색은 부분 적용입니다. 원하지 않는 UV 영역에서 땀이 나오지 않게 할 수 있습니다. | UV0와 R 채널을 사용합니다. |
| **표시 마스크** | **기본 > 표시 마스크** | 물방울·흐른 자국·젖은 막·광택·굴절·어두워짐과 Tess 변위를 포함한 표면 최종 결과를 숨깁니다. | UV0, R/G/B/A 선택·반전·강도·타일링·오프셋을 지원합니다. 회색조 값은 **sRGB 끄기**. 기본 강도 `0`은 비활성입니다. 낙하 물방울에는 적용되지 않습니다. |
| **흐름 가이드 맵 (선택)** | **흐름 & 자국 > 흐름 가이드 맵 (선택)** | UV0 탄젠트 방향으로 흐르는 물방울과 자국을 유도합니다. | RG 데이터 텍스처이며 중립은 `(0.5, 0.5)` 회색입니다. **sRGB 끄기**, **Generate Mip Maps 켜기**. 강도 `0`은 월드 중력만 사용하고 가이드 맵을 샘플하지 않으며 시작 권장은 `0.35–0.65`입니다. |
| **바디 노멀맵 (주름 고임 / 광택 전용)** | **고급 > 바디 노멀 — 젖은 표현 전용** | 젖은 하이라이트와 주름의 고임 표현을 바꿉니다. | 물방울이나 자국의 방향을 바꾸지 않습니다. 방향에는 Flow Guide Map을 사용하세요. |

## 상대 저부하와 거울

**상대 저부하(Remote Low Load)**는 아바타 메뉴 옵션이며 Tuning Inspector 항목은 아닙니다. 테셀레이션 지원 여부와 관계없이 설치됩니다.

- Tess 지원 시 상대방은 각 대상의 No Tess 머티리얼과 내장 Lite 경로를 사용합니다.
- Tess 미지원 시 각 대상의 같은 No Tess 머티리얼이 내장 Lite 경로로 전환됩니다.
- 3D 파티클 품질에서는 상대 낙하 물방울을 50%로 줄이고 강한 움직임 추가 방울을 끄며, 상대 몸 김／Custom Steam을 25%로 줄입니다.
- 별도 “Performance material”을 사용하지 않으며, 소유자 본인의 로컬 화면을 낮추지도 않습니다.

기본 거울 카메라는 저비용 광학 경로를 사용하여 일부 근접 표면 표현을 단순화합니다. Setup/Tuning의 **거울에서도 바디 땀 전체 품질**을 켜면 물방울 밀도·광학·Light Volumes·Tess/No Tess를 메인 화면과 같게 복원하지만 거울 GPU 부하가 증가합니다. 몸 김은 별도로 `Full / Low / Hidden`을 선택합니다.

:::warning[GPU 부하가 겹치는 조합]
많은 땀양, 높은 패턴 밀도, 화면을 크게 차지하는 근접 화면, 거울, 테셀레이션을 함께 사용하면 GPU 부하가 누적됩니다. 큰 옵션은 한 번에 하나씩 바꾸고 업로드된 아바타로 확인하세요.
:::

## 다음 문서

- 저부하 옵션 비교: [성능 가이드](../performance.mdx)
- 렌더 순서와 머티리얼 조건: [호환성](../compatibility.mdx)
- 값이 반영되지 않을 때: [문제 해결](../troubleshooting.mdx#tuning-값을-바꿨지만-반영되지-않음)
